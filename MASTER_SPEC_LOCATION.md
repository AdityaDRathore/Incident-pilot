# Master specification

Save the previously supplied IncidentPilot master build prompt in this repository as:

`docs/master-spec.md`

The custom agents are instructed to read this file before making major changes.
